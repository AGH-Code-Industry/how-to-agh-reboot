[build]
  command = "pnpm build"
  publish = "dist"

[context.deploy-preview]
  command = "npx prisma generate && pnpm push && pnpm seed && pnpm run build"

[context.production]
  command = "npx prisma generate && npx prisma migrate deploy && pnpm run build"
